Two-to-four-way switch between sibling views — Spending/Savings, week/month ranges, parent/kid preview.

```jsx
<SegmentedControl options={[{value:'spend',label:'Spending',icon:'wallet'},{value:'save',label:'Savings',icon:'piggy-bank'}]} value={tab} onChange={setTab} />
```
