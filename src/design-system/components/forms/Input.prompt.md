Standard text field with label, hint, and error states.

```jsx
<Input label="Child's name" placeholder="e.g. Maya" value={v} onChange={set} />
<Input label="Email" icon="mail" error="That email is already used" />
```
