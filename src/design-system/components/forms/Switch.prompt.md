Toggle for parent-controlled feature unlocks (savings, goals, loans, credit) and notification opt-ins.

```jsx
<Switch checked={on} onChange={setOn} label="Savings interest" description="Maya will start earning 5% a year." />
```

Always write the description from the child's point of view — the parent should know exactly what unlocking changes in kid mode.
