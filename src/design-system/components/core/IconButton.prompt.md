Circular icon-only button for headers, rows, and toolbars.

```jsx
<IconButton icon="settings" label="Settings" variant="onBrand" />
```

Use `onBrand` inside coloured headers, `soft` on white surfaces, `outline` in parent-mode tables. Never below 36px; default 44px meets the kid-safe hit target.
