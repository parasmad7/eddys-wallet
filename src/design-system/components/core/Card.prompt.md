The universal surface: rounded, hairline-bordered, soft violet shadow. Radius follows the active mode theme (rounder in kid mode).

```jsx
<Card tone="plain" pad="md" interactive onClick={open}>…</Card>
```

Tones map to meaning: `mint` for savings/growth, `accent` for goals and celebrations, `brand` for the hero balance, `sunken` for grouped lists.
