Short clarifier on hover/focus — parent mode only (kids get `EddyTip` instead, since tooltips need a pointer).

```jsx
<Tooltip label="500 basis points = 5.00%"><Icon name="info" size={14}/></Tooltip>
```
