Zero-state for empty ledgers, no goals yet, no loans. Kid mode uses the mascot; parent mode uses the icon circle.

```jsx
<EmptyState mascotSrc="assets/eddy-mascot.svg" title="No goals yet" action={<Button icon="plus">Add a goal</Button>}>
  Pick something you're saving for and watch it fill up.
</EmptyState>
```
