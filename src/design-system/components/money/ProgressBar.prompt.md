Goal and loan payoff progress. Fill animates on change with the out-ease.

```jsx
<ProgressBar value={3200} max={5000} tone="accent" label="New bike" showPct />
```

Pass cents directly for value/max. Use `accent` for savings goals, `mint` for savings growth, `berry` for debt remaining.
